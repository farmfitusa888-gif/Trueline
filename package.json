{
  "name": "trueline",
  "private": true,
  "version": "0.1.0",
  "description": "Trueline \u2014 scan a building, correct it by hand, get a blueprint.",
  "workspaces": [
    "core",
    "web"
  ],
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "test": "npm run test -w core",
    "typecheck": "npm run typecheck -w core && npm run typecheck -w web",
    "dev": "npm run dev -w web",
    "build": "npm run build -w web",
    "audit": "npm run build --workspace=@trueline/web && node web/audit/run.mjs",
    "check-guide": "python3 docs/build/check-guide.py docs/handbook.html",
    "what-is-left": "python3 core/tools/check-reachable.py",
    "verify": "npm test && npm run typecheck && npm run build && npm run check-guide && npm run what-is-left && npm run check-swift && npm run check-the-checks",
    "ship-web": "npm run build && rm -rf ios/Trueline/Web && mkdir -p ios/Trueline/Web && cp -R web/dist/. ios/Trueline/Web/",
    "check-swift": "python3 core/tools/check-swift.py && python3 core/tools/check-swift-names.py && python3 core/tools/check-pbxproj.py",
    "check-the-checks": "python3 core/tools/check-the-checks.py"
  }
}
