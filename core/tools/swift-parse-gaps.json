{
  "Backup.swift": {
    "sha256": "68e33c2fe7b00f99eacee75b0197b66ffaa6e74d2c445c992816120b2bebfbf4",
    "why": "uses `switch try await`, `if let x = try? await y()` and a nil-coalesced optional cast in a guard -- all valid Swift the tree-sitter grammar rejects. Read line by line on 2026-08-25. Hash updated the same day for one added line, `import Combine`: the file uses ObservableObject and @Published, which live in Combine and were never imported. Hash updated again on 2026-08-26 for the `kind` field: a scan, a walk and a drawing are three formats sharing one `capture` field, and without a word saying which, every restored capture was written to room.json. The whole diff -- one parameter, one record key, one struct field, one read with a `scanned` default for records written before it existed -- was read before the hash was moved, which is the whole point of the hash. Hash updated on 2026-08-27 for the `card` field, which carries a room's name, job and archive flag to iCloud so a second phone does not restore every room called \"Room 2026-08-26 0927\" again. The whole diff -- one parameter, one guarded record key, one struct field, one optional read -- was read before the hash was moved. Hash updated on 2026-08-27 for `lastRestore`: restoring from iCloud ran once at launch and said nothing whatever happened, so a room deleted by accident and not restored looked identical to three different problems -- no copy in iCloud, a query iCloud refused, and a restore that worked and found nothing missing. The whole diff -- one @Published string, one line clearing it, one sentence on success and one on failure -- was read before the hash was moved. Hash updated on 2026-09-03 for the rename to ScanToBid: the whole diff is TWO lines and neither is code -- one doc comment saying there is no ScanToBid server, and one sentence shown to somebody whose iCloud refused the copy, telling them which app to switch on in iCloud Drive. Both were read before the hash was moved, which is the whole point of the hash."
  }
}
